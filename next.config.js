/** @type {import('next').NextConfig} */
const nextConfig = {
  // Aquí van tus opciones de configuración, por ejemplo:
  reactStrictMode: true,
  swcMinify: true,
  // Agrega cualquier otra opción que necesites
};

module.exports = nextConfig;
